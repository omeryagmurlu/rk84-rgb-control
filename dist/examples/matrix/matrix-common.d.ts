import { RGB } from "../../..";
export declare const createMatrix: (r: number, c: number) => RGB[][];
export declare const transpose: <T>(m: T[][]) => T[][];
export declare const lo: (x: RGB[][]) => void;
